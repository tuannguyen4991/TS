{
  "name": "sort",
  "version": "1.0.0",
  "scripts": {
    "start:build": "tsc",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "dependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2",
    "typescript-closure-compiler": "^1.8.11"
  }
}
